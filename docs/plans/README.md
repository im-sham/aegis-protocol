# Historical Plans

This directory contains implementation plans and design notes from earlier AEGIS development phases.

Files here may preserve old testnet addresses, package versions, or planned APIs for historical context. Current public deployment addresses live in the root `README.md`, `CLAUDE.md`, `TODO.md`, and `subgraph/config/base-sepolia.json`.
