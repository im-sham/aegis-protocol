{
  "name": "aegis-protocol-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "AEGIS Protocol TypeScript SDK monorepo",
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "generate:abis": "pnpm --filter @aegis-protocol/abis generate"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.4"
}
